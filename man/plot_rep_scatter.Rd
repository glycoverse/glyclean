% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{plot_rep_scatter}
\alias{plot_rep_scatter}
\title{Plot Replicate Scatter Plots}
\usage{
plot_rep_scatter(exp, rep_col, n_pairs = 9)
}
\arguments{
\item{exp}{A \code{\link[glyexp:GlycomicSE]{glyexp::GlycomicSE()}}, \code{\link[glyexp:GlycoproteomicSE]{glyexp::GlycoproteomicSE()}}, or
\code{\link[SummarizedExperiment:SummarizedExperiment-class]{SummarizedExperiment::SummarizedExperiment()}} object.}

\item{rep_col}{Column name in \code{sample_info} used to define replicate groups.
Samples with the same value in this column are treated as replicates
(e.g. \code{c("A", "A", "A", "B", "B", "B")} indicates three replicates for
sample A and three for sample B).}

\item{n_pairs}{Number of replicate pairs to draw at random.}
}
\value{
A patchwork object containing replicate scatter plots.
}
\description{
Randomly draw replicate sample pairs and plot log2 intensity scatter plots.
The plot title shows sample names, and the subtitle reports the R2 value.
}
\examples{
library(SummarizedExperiment)

exp <- glyexp::real_experiment
replicate <- rep(c("A", "B"), length.out = ncol(exp))
colData(exp)$replicate <- replicate
plot_rep_scatter(exp, rep_col = "replicate", n_pairs = 4)

}
