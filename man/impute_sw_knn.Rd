% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute.R
\name{impute_sw_knn}
\alias{impute_sw_knn}
\alias{impute_sw_knn.glyexp_experiment}
\alias{impute_sw_knn.matrix}
\alias{impute_sw_knn.default}
\title{Sample-wise KNN Imputation}
\usage{
impute_sw_knn(x, k = 5, by = NULL, ...)

\method{impute_sw_knn}{glyexp_experiment}(x, k = 5, by = NULL, ...)

\method{impute_sw_knn}{matrix}(x, k = 5, by = NULL, ...)

\method{impute_sw_knn}{default}(x, k = 5, by = NULL, ...)
}
\arguments{
\item{x}{Either a \code{glyexp_experiment} object or a matrix.
If a matrix, rows should be variables and columns should be samples.}

\item{k}{The number of nearest neighbors to consider.}

\item{by}{Either a column name in \code{sample_info} (string) or a factor/vector
specifying group assignments for each sample. Used for grouping when imputing missing values.}

\item{...}{Additional arguments to pass to \code{impute::impute.knn()}.}
}
\value{
Returns the same type as the input. If \code{x} is a \code{glyexp_experiment},
returns a \code{glyexp_experiment} with missing values imputed.
If \code{x} is a matrix, returns a matrix with missing values imputed.
}
\description{
A wrapper around the \code{\link[impute:impute.knn]{impute::impute.knn()}}.
Impute missing values with values from the k-nearest neighbors of the
corresponding sample.
If there are strong patterns among the samples (such as group clustering
relationships or experimental conditions), this method can better utilize
the overall relationships among samples.
}
